#include <stdio.h>
/**
 * main -entry point
 * desc: print alphabet
 * Return:0
 */
int main(void)
{
void print_alphabet(void)
{
	char a;
	
	for (a = 'a'; a <= 'z'; a++)
		putchar(a);
	putchar('\n');
}
	return (0);
	}
